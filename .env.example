PORT=3000
BASE_URL=http://localhost:3000
SESSION_SECRET=replace-with-a-long-random-string

# Optional real SMTP config. If omitted, Nodemailer creates an Ethereal test inbox.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Passwordless Auth <no-reply@example.com>"